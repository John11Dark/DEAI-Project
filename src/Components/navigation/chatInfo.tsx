import React from "react";

type MessageInfoProps = {
  className?: string;
};
export default function MessageInfo({ className }: MessageInfoProps) {
  return <div className="chat-info-container">MessageInfo</div>;
}
